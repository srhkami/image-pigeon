# 更新日誌

本文件記錄貼圖小鴿手各版本的重要變更。

## [3.0.0] - 2026-08-25

### 重大更新

- 將圖片匯入、壓縮、長截圖切割、專案管理與檔案輸出改為純前端處理；除既有版本檢查外，圖片與專案資料不會傳送到網路服務。
- 專案交換格式統一為單一 `.ipigeon` 壓縮檔，內含 `project.json` 與 `images/*.webp`。
- 停止支援 1.x 獨立 JSON 專案檔及舊桌面版 `.ipigeon/` 專案資料夾；3.0.0 不提供舊格式轉換工具。

### 新增

- 在瀏覽器本機完成一般圖片壓縮與長截圖切割，並顯示處理進度、略過項目及安全錯誤摘要。
- 可直接下載 Word 文件、圖片 ZIP 與 `.ipigeon` 專案檔，不再依賴桌面版檔案橋接。
- 新增全頁 A4 列印預覽，可透過系統列印對話框列印或另存 PDF。
- 新增專案封存的 ZIP 路徑、格式、WebP 內容、圖片尺寸、資產一致性及資源上限驗證。

### 改善

- 改善取消與全部清除行為：已取消或晚到的非同步結果不會覆寫目前專案。
- 改善瀏覽器資產生命週期，替換、刪除、清除與離開頁面時會回收本機圖片資源。
- 圖片 ZIP 與 Word 輸出依目前專案順序產生，並處理旋轉、檔名正規化、重複名稱及資源上限。
- 保留既有五種自動拼貼版型、焦點式編輯、排序模式、備註、旋轉與直向圖片大小設定。

### 注意事項

- 3.0.0 只能開啟新版單一 `.ipigeon` 專案檔；升級前請確認不再需要舊 JSON 或舊專案資料夾。
- 另存 PDF 由瀏覽器與作業系統的列印對話框提供，應用程式無法指定儲存路徑或繞過裝置管理政策。
- Windows 11、Microsoft Edge 與 Microsoft 365 Word 的完整目標環境驗收尚未執行；本版不宣稱已通過該相容性矩陣。
